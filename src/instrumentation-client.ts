/**
 * Client boot for hero SVG sketch-in. pathLength="1" + dash offset 1→0
 * so every stroke finishes (a 920-unit dash used to cut the frame short).
 */
import "./components/sections/Hero/heroSketchRuntime";
